# arcade-ai Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 一个 npm 包 `arcade-ai`（CLI `aca`），用 `init` 起 MakeCode Arcade 脚手架、`dev` 起本地 studio（嵌官方编辑器、双向实时同步）、`check` 自检 postMessage 协议，并随包提供 skill 与参考手册。

**Architecture:** 单一 npm 包。CLI 三命令调用 `src/commands/*`。`dev` 起一个内置 `http` 服务：托管 host 页 + `GET /api/project` / `POST /api/save` + `GET /events`(SSE)，用 chokidar 监听 `game/` 目录变化经 SSE 通知页面程序化 reload iframe。编译发生在官方浏览器编辑器，本地无 pxt 工具链。

**Tech Stack:** Node ≥18（ESM, `"type":"module"`）；内置 `node:test` / `node:assert` / `node:http` / `node:util.parseArgs`；运行依赖 `chokidar`；`puppeteer` 为 optionalDependencies（仅 `check`，懒加载）。

## Global Constraints

- Node 版本下限：**>=18.0.0**（用到 `util.parseArgs`、内置 fetch、`node:test`）。
- 模块格式：ESM（package.json `"type": "module"`），所有 import 用 `.js` 扩展名。
- 运行依赖只允许：`chokidar`。`puppeteer` 只能放 `optionalDependencies`，且仅在 `check` 命令内 `await import()` 懒加载。
- 项目文件白名单（读写都用这个，顺序固定）：`['README.md','main.ts','main.blocks','pxt.json','assets.json','test.ts']`。
- 4 个必须固化进模板的坑（逐字）：
  1. `assets.json` 内容必须是合法 `"{}"`，不得为空文件。
  2. `pxt.json` 的 `files` 数组必须含 `"README.md","main.ts","main.blocks","assets.json"`。
  3. 推送给编辑器的 header 用 `editor: "blocksprj"`。
  4. iframe.src 用 `https://arcade.makecode.com/?controller=1`（根域名，不带 `index.html`）。
- 包名 `arcade-ai`；bin 命令 `aca`；`makecode` 只进 package.json 的 `keywords`/`description`，不进包名。
- 提交粒度：每个 Task 末尾提交一次。提交信息末尾加 `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`。

---

## File Structure

```
arcade-ai/                         # 仓库根 = npm 包根（即 /home/guolin/workspace/pxt）
  package.json                     # bin.aca, type:module, deps, keywords
  bin/aca.js                       # CLI 入口：解析 argv → run()
  src/
    cli.js                         # parseArgs + run 分发表
    project-io.js                  # readProject / writeProject（白名单 + 写盘回调）
    studio/
      watcher.js                   # createWatcher（chokidar + pause 防抖回声）
      server.js                    # startStudio（http: host页 + api + SSE）
    commands/
      init.js                      # runInit：拷贝模板 + 生成规则文件
      dev.js                       # runDev：起 studio + 开浏览器
      check.js                     # runCheck：puppeteer 自检
    host/
      index.html                   # 宿主页（workspacesync + SSE reload）
    template/                      # init 拷贝的静态脚手架
      game/{README.md,main.ts,main.blocks,pxt.json,assets.json}
      package.json                 # 生成项目的 package.json
    rules/
      claude.md / project_rules.md / agents.md   # 多格式规则模板
    reference/
      arcade-api.md / limits.md / pitfalls.md / project-format.md
  SKILL.md                         # skill 本体
  README.md                        # 分工具安装说明
  tests/
    cli.test.js / project-io.test.js / init.test.js
    server.test.js / reference.test.js
```

---

## Task 1: 包骨架与 CLI 分发

**Files:**
- Create: `package.json`
- Create: `bin/aca.js`
- Create: `src/cli.js`
- Create: `src/commands/init.js`, `src/commands/dev.js`, `src/commands/check.js`（占位，仅导出函数）
- Test: `tests/cli.test.js`

**Interfaces:**
- Produces: `src/cli.js` 导出 `parseArgs(argv) -> { command, positionals, options }` 和 `run(argv) -> Promise<number>`（返回退出码）。`run` 用分发表 `{ init, dev, check }` 调对应 `src/commands/<name>.js` 的默认导出。
- Produces: 三个命令模块默认导出 `async (ctx) => number`，ctx = `{ positionals, options }`。

- [ ] **Step 1: 写失败测试**

```js
// tests/cli.test.js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { parseArgs, run } from '../src/cli.js';

test('parseArgs 解析命令与选项', () => {
  const r = parseArgs(['init', 'my-game', '--tool', 'claude']);
  assert.equal(r.command, 'init');
  assert.deepEqual(r.positionals, ['my-game']);
  assert.equal(r.options.tool, 'claude');
});

test('run 未知命令返回退出码 1', async () => {
  const code = await run(['frobnicate']);
  assert.equal(code, 1);
});

test('run 无命令打印帮助返回 0', async () => {
  const code = await run([]);
  assert.equal(code, 0);
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `node --test tests/cli.test.js`
Expected: FAIL（`Cannot find module '../src/cli.js'`）

- [ ] **Step 3: 写最小实现**

```json
// package.json
{
  "name": "arcade-ai",
  "version": "0.1.0",
  "description": "AI-driven scaffold and live studio for Microsoft MakeCode Arcade games",
  "keywords": ["makecode", "arcade", "ai", "game", "pixel", "education"],
  "type": "module",
  "bin": { "aca": "bin/aca.js" },
  "engines": { "node": ">=18.0.0" },
  "files": ["bin", "src", "SKILL.md", "README.md"],
  "dependencies": { "chokidar": "^3.6.0" },
  "optionalDependencies": { "puppeteer": "^22.0.0" },
  "scripts": { "test": "node --test" }
}
```

```js
// bin/aca.js
#!/usr/bin/env node
import { run } from '../src/cli.js';
run(process.argv.slice(2)).then((code) => process.exit(code));
```

```js
// src/cli.js
import { parseArgs as nodeParseArgs } from 'node:util';

const COMMANDS = { init: 'init', dev: 'dev', check: 'check' };

export function parseArgs(argv) {
  const { values, positionals } = nodeParseArgs({
    args: argv,
    allowPositionals: true,
    strict: false,
    options: {
      tool: { type: 'string' },
      port: { type: 'string' },
      url: { type: 'string' },
    },
  });
  const [command, ...rest] = positionals;
  return { command, positionals: rest, options: values };
}

const HELP = `arcade-ai (aca)
用法:
  aca init [dir] [--tool claude|trae|agents]   起脚手架
  aca dev  [--port 8080]                        起本地 studio
  aca check [--url <makecode-url>]              自检 postMessage 协议`;

export async function run(argv) {
  const { command, positionals, options } = parseArgs(argv);
  if (!command) { console.log(HELP); return 0; }
  if (!COMMANDS[command]) { console.error(`未知命令: ${command}\n\n${HELP}`); return 1; }
  const mod = await import(`./commands/${command}.js`);
  return mod.default({ positionals, options });
}
```

```js
// src/commands/init.js
export default async function init(ctx) { console.log('init stub', ctx.positionals); return 0; }
```

```js
// src/commands/dev.js
export default async function dev(ctx) { console.log('dev stub'); return 0; }
```

```js
// src/commands/check.js
export default async function check(ctx) { console.log('check stub'); return 0; }
```

- [ ] **Step 4: 运行测试确认通过**

Run: `node --test tests/cli.test.js`
Expected: PASS（3 tests）

- [ ] **Step 5: 提交**

```bash
git add package.json bin/ src/ tests/cli.test.js
git commit -m "feat: arcade-ai 包骨架与 CLI 分发"
```

---

## Task 2: project-io 读写白名单

**Files:**
- Create: `src/project-io.js`
- Test: `tests/project-io.test.js`

**Interfaces:**
- Consumes: 无。
- Produces:
  - `PROJECT_FILES: string[]`（白名单，见 Global Constraints）。
  - `readProject(gameDir) -> { [filename]: string }`：仅返回存在的白名单文件。
  - `writeProject(gameDir, files, opts?) -> string[]`：只写白名单内文件，写每个文件前调用 `opts.beforeWrite?.()`，返回实际写入的文件名数组。

- [ ] **Step 1: 写失败测试**

```js
// tests/project-io.test.js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { mkdtemp, writeFile, readFile } from 'node:fs/promises';
import { tmpdir } from 'node:os';
import { join } from 'node:path';
import { readProject, writeProject, PROJECT_FILES } from '../src/project-io.js';

async function tmp() { return mkdtemp(join(tmpdir(), 'aca-')); }

test('readProject 只返回存在的白名单文件', async () => {
  const dir = await tmp();
  await writeFile(join(dir, 'main.ts'), 'let x = 1');
  await writeFile(join(dir, 'secret.env'), 'NOPE');
  const files = readProject(dir);
  assert.equal(files['main.ts'], 'let x = 1');
  assert.ok(!('secret.env' in files));
});

test('writeProject 过滤非白名单并回调 beforeWrite', async () => {
  const dir = await tmp();
  let calls = 0;
  const written = writeProject(dir, { 'main.ts': 'A', 'evil.sh': 'rm' }, { beforeWrite: () => calls++ });
  assert.deepEqual(written, ['main.ts']);
  assert.equal(calls, 1);
  assert.equal(await readFile(join(dir, 'main.ts'), 'utf8'), 'A');
});

test('PROJECT_FILES 含四坑约束文件', () => {
  for (const f of ['README.md','main.ts','main.blocks','pxt.json','assets.json']) {
    assert.ok(PROJECT_FILES.includes(f), `缺 ${f}`);
  }
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `node --test tests/project-io.test.js`
Expected: FAIL（`Cannot find module '../src/project-io.js'`）

- [ ] **Step 3: 写最小实现**

```js
// src/project-io.js
import { readFileSync, writeFileSync, existsSync } from 'node:fs';
import { join } from 'node:path';

export const PROJECT_FILES = ['README.md', 'main.ts', 'main.blocks', 'pxt.json', 'assets.json', 'test.ts'];

export function readProject(gameDir) {
  const out = {};
  for (const f of PROJECT_FILES) {
    const p = join(gameDir, f);
    if (existsSync(p)) out[f] = readFileSync(p, 'utf8');
  }
  return out;
}

export function writeProject(gameDir, files, opts = {}) {
  const written = [];
  for (const f of Object.keys(files || {})) {
    if (!PROJECT_FILES.includes(f)) continue;
    opts.beforeWrite?.();
    writeFileSync(join(gameDir, f), files[f] ?? '', 'utf8');
    written.push(f);
  }
  return written;
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `node --test tests/project-io.test.js`
Expected: PASS（3 tests）

- [ ] **Step 5: 提交**

```bash
git add src/project-io.js tests/project-io.test.js
git commit -m "feat: project-io 读写白名单"
```

---

## Task 3: 静态模板与 init 拷贝

**Files:**
- Create: `src/template/game/README.md`
- Create: `src/template/game/main.ts`
- Create: `src/template/game/main.blocks`
- Create: `src/template/game/pxt.json`
- Create: `src/template/game/assets.json`
- Create: `src/template/package.json`
- Modify: `src/commands/init.js`
- Test: `tests/init.test.js`

**Interfaces:**
- Consumes: 无（模板为静态文件）。
- Produces: `runInit(ctx)` 在 `ctx.positionals[0] || 'arcade-game'` 目录下生成 `game/` + `package.json`。导出 `templateDir` 常量供测试定位。

- [ ] **Step 1: 写失败测试**

```js
// tests/init.test.js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { mkdtemp, readFile } from 'node:fs/promises';
import { tmpdir } from 'node:os';
import { join } from 'node:path';
import runInit from '../src/commands/init.js';

test('init 生成 game 目录且固化四个坑', async () => {
  const base = await mkdtemp(join(tmpdir(), 'aca-init-'));
  const dest = join(base, 'my-game');
  const code = await runInit({ positionals: [dest], options: {} });
  assert.equal(code, 0);

  const pxt = JSON.parse(await readFile(join(dest, 'game/pxt.json'), 'utf8'));
  for (const f of ['README.md','main.ts','main.blocks','assets.json']) {
    assert.ok(pxt.files.includes(f), `pxt.json files 缺 ${f}`);   // 坑2
  }
  const assets = await readFile(join(dest, 'game/assets.json'), 'utf8');
  assert.equal(assets.trim(), '{}');                              // 坑1
  await readFile(join(dest, 'game/main.ts'), 'utf8');             // 存在即可
  await readFile(join(dest, 'package.json'), 'utf8');
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `node --test tests/init.test.js`
Expected: FAIL（断言 game/pxt.json 不存在 → readFile 抛错）

- [ ] **Step 3: 写模板文件**

```jsonc
// src/template/game/pxt.json
{
  "name": "arcade-game",
  "version": "0.0.1",
  "dependencies": { "device": "*" },
  "description": "",
  "files": ["README.md", "main.ts", "main.blocks", "assets.json"],
  "preferredEditor": "blocksprj"
}
```

```json
// src/template/game/assets.json
{}
```

```ts
// src/template/game/main.ts
let player = sprites.create(img`
. . . . . . . . . . . . . . . .
. . . . . . . f f . . . . . . .
. . . . . . f 4 4 f . . . . . .
. . . . . . f 4 4 f . . . . . .
. . . . . f 4 4 4 4 f . . . . .
. . . . f 4 4 4 4 4 4 f . . . .
. . . . f 4 4 4 4 4 4 f . . . .
. . . . f 4 4 4 4 4 4 f . . . .
`, SpriteKind.Player)
controller.moveSprite(player)
player.setStayInScreen(true)
```

```text
<!-- src/template/game/main.blocks -->
<xml xmlns="https://developers.google.com/blockly/xml"></xml>
```

```markdown
<!-- src/template/game/README.md -->
# Arcade Game
由 arcade-ai 脚手架生成。用 AI 修改 `main.ts`，`aca dev` 实时预览。
```

```json
// src/template/package.json
{
  "name": "arcade-game-project",
  "private": true,
  "type": "module",
  "scripts": { "dev": "aca dev" },
  "devDependencies": { "arcade-ai": "^0.1.0" }
}
```

- [ ] **Step 4: 实现 init 拷贝逻辑**

```js
// src/commands/init.js
import { cpSync, mkdirSync, copyFileSync } from 'node:fs';
import { join, dirname } from 'node:path';
import { fileURLToPath } from 'node:url';

const here = dirname(fileURLToPath(import.meta.url));
export const templateDir = join(here, '..', 'template');

export default async function init(ctx) {
  const dest = ctx.positionals[0] || 'arcade-game';
  mkdirSync(dest, { recursive: true });
  cpSync(join(templateDir, 'game'), join(dest, 'game'), { recursive: true });
  copyFileSync(join(templateDir, 'package.json'), join(dest, 'package.json'));
  console.log(`✅ 已创建 ${dest}\n  cd ${dest} && npx aca dev`);
  return 0;
}
```

- [ ] **Step 5: 运行测试确认通过**

Run: `node --test tests/init.test.js`
Expected: PASS

- [ ] **Step 6: 提交**

```bash
git add src/template/ src/commands/init.js tests/init.test.js
git commit -m "feat: 静态模板与 init 拷贝（固化四坑）"
```

---

## Task 4: init 多格式规则文件生成

**Files:**
- Create: `src/rules/claude.md`, `src/rules/project_rules.md`, `src/rules/agents.md`
- Modify: `src/commands/init.js`
- Test: `tests/init.test.js`（追加用例）

**Interfaces:**
- Consumes: `templateDir`（Task 3）。
- Produces: `runInit` 依 `--tool` 写规则文件：`claude`→`CLAUDE.md`、`trae`→`.trae/project_rules.md`、默认/`agents`→`AGENTS.md`。规则源文件内容相同（同一份给 AI 的项目规则），仅文件名/位置不同。

- [ ] **Step 1: 追加失败测试**

```js
// tests/init.test.js（追加）
import { stat } from 'node:fs/promises';

test('init --tool claude 写 CLAUDE.md', async () => {
  const base = await mkdtemp(join(tmpdir(), 'aca-tool-'));
  const dest = join(base, 'g');
  await runInit({ positionals: [dest], options: { tool: 'claude' } });
  await stat(join(dest, 'CLAUDE.md'));
});

test('init 默认写 AGENTS.md', async () => {
  const base = await mkdtemp(join(tmpdir(), 'aca-tool2-'));
  const dest = join(base, 'g');
  await runInit({ positionals: [dest], options: {} });
  await stat(join(dest, 'AGENTS.md'));
});

test('init --tool trae 写 .trae/project_rules.md', async () => {
  const base = await mkdtemp(join(tmpdir(), 'aca-tool3-'));
  const dest = join(base, 'g');
  await runInit({ positionals: [dest], options: { tool: 'trae' } });
  await stat(join(dest, '.trae/project_rules.md'));
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `node --test tests/init.test.js`
Expected: FAIL（CLAUDE.md 不存在）

- [ ] **Step 3: 写规则源文件**

`src/rules/agents.md`（`claude.md`、`project_rules.md` 内容相同，可直接复制同一文本）：

```markdown
# 给 AI 的项目规则：MakeCode Arcade 游戏

- 游戏代码只写在 `game/main.ts`；资源（精灵/地图，4-bit 16 色）走 `game/assets.json`，不要内联大图到 JS。
- 改完代码后 `aca dev` 的页面会自动刷新预览；无需手动操作。
- 严守 MakeCode Arcade 限制：见随包 `reference/limits.md`；API 速查见 `reference/arcade-api.md`。
- 不要改 `game/pxt.json` 的 `files` 数组、不要清空 `assets.json`（必须是合法 JSON）。
- 不确定 API 时查 `reference/`，不要臆造不存在的 arcade API。
```

- [ ] **Step 4: 实现规则写入**

```js
// src/commands/init.js（在 console.log 之前插入）
import { readFileSync, writeFileSync } from 'node:fs';

const RULES = {
  claude: { src: 'claude.md', dest: 'CLAUDE.md' },
  trae: { src: 'project_rules.md', dest: '.trae/project_rules.md' },
  agents: { src: 'agents.md', dest: 'AGENTS.md' },
};

function writeRules(dest, tool) {
  const r = RULES[tool] || RULES.agents;
  const content = readFileSync(join(here, '..', 'rules', r.src), 'utf8');
  const target = join(dest, r.dest);
  mkdirSync(dirname(target), { recursive: true });
  writeFileSync(target, content, 'utf8');
}
```

在 `init` 默认导出里 `copyFileSync(...)` 之后调用：`writeRules(dest, ctx.options.tool);`

- [ ] **Step 5: 运行测试确认通过**

Run: `node --test tests/init.test.js`
Expected: PASS（全部 init 用例）

- [ ] **Step 6: 提交**

```bash
git add src/rules/ src/commands/init.js tests/init.test.js
git commit -m "feat: init 多格式规则文件生成"
```

---

## Task 5: watcher（chokidar + pause 防回声）

**Files:**
- Create: `src/studio/watcher.js`
- Test: `tests/watcher.test.js`

**Interfaces:**
- Consumes: 无。
- Produces: `createWatcher(gameDir, onChange) -> { close(): Promise<void>, pause(ms): void }`。`onChange()` 在 `gameDir` 任意文件增改时触发（200ms 防抖）；`pause(ms)` 后的 ms 毫秒内忽略事件（防自身写盘回声）。

- [ ] **Step 1: 写失败测试**

```js
// tests/watcher.test.js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { mkdtemp, writeFile } from 'node:fs/promises';
import { tmpdir } from 'node:os';
import { join } from 'node:path';
import { createWatcher } from '../src/studio/watcher.js';

const wait = (ms) => new Promise((r) => setTimeout(r, ms));

test('文件变化触发 onChange', async () => {
  const dir = await mkdtemp(join(tmpdir(), 'aca-w-'));
  let hits = 0;
  const w = createWatcher(dir, () => hits++);
  await wait(300);                     // 等 chokidar ready
  await writeFile(join(dir, 'main.ts'), 'v1');
  await wait(500);
  assert.ok(hits >= 1, `期望至少 1 次，实际 ${hits}`);
  await w.close();
});

test('pause 期间忽略变化', async () => {
  const dir = await mkdtemp(join(tmpdir(), 'aca-w2-'));
  let hits = 0;
  const w = createWatcher(dir, () => hits++);
  await wait(300);
  w.pause(600);
  await writeFile(join(dir, 'main.ts'), 'v2');
  await wait(400);
  assert.equal(hits, 0);
  await w.close();
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `node --test tests/watcher.test.js`
Expected: FAIL（找不到模块）

- [ ] **Step 3: 写最小实现**

```js
// src/studio/watcher.js
import chokidar from 'chokidar';

export function createWatcher(gameDir, onChange) {
  let pausedUntil = 0;
  let timer = null;
  const watcher = chokidar.watch(gameDir, { ignoreInitial: true });
  const fire = () => {
    if (Date.now() < pausedUntil) return;
    clearTimeout(timer);
    timer = setTimeout(() => { if (Date.now() >= pausedUntil) onChange(); }, 200);
  };
  watcher.on('add', fire).on('change', fire);
  return {
    pause(ms) { pausedUntil = Date.now() + ms; },
    async close() { clearTimeout(timer); await watcher.close(); },
  };
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `npm install && node --test tests/watcher.test.js`
Expected: PASS（2 tests）

- [ ] **Step 5: 提交**

```bash
git add src/studio/watcher.js tests/watcher.test.js package-lock.json
git commit -m "feat: studio watcher（chokidar + pause 防回声）"
```

---

## Task 6: studio 服务（host 页 + /api/project + /api/save + SSE）

**Files:**
- Create: `src/studio/server.js`
- Test: `tests/server.test.js`

**Interfaces:**
- Consumes: `readProject`/`writeProject`（Task 2）、`createWatcher`（Task 5）。
- Produces: `startStudio({ gameDir, port = 0, hostHtmlPath }) -> Promise<{ url, port, close() }>`。路由：`GET /` → 读 `hostHtmlPath`；`GET /api/project` → `{files}`；`POST /api/save`（body `{files}`）→ writeProject 并 `watcher.pause(600)`；`GET /events` → SSE，watcher onChange 时推 `data: changed\n\n`。

- [ ] **Step 1: 写失败测试**

```js
// tests/server.test.js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { mkdtemp, writeFile } from 'node:fs/promises';
import { tmpdir } from 'node:os';
import { join } from 'node:path';
import { startStudio } from '../src/studio/server.js';

const wait = (ms) => new Promise((r) => setTimeout(r, ms));

test('/api/project 返回项目文件，/api/save 写盘', async () => {
  const dir = await mkdtemp(join(tmpdir(), 'aca-s-'));
  await writeFile(join(dir, 'main.ts'), 'orig');
  const hostHtml = join(dir, 'host.html');
  await writeFile(hostHtml, '<html>host</html>');
  const s = await startStudio({ gameDir: dir, hostHtmlPath: hostHtml });

  const proj = await (await fetch(`${s.url}/api/project`)).json();
  assert.equal(proj.files['main.ts'], 'orig');

  const r = await fetch(`${s.url}/api/save`, {
    method: 'POST', headers: { 'content-type': 'application/json' },
    body: JSON.stringify({ files: { 'main.ts': 'edited' } }),
  });
  assert.equal((await r.json()).success, true);
  const proj2 = await (await fetch(`${s.url}/api/project`)).json();
  assert.equal(proj2.files['main.ts'], 'edited');

  const home = await (await fetch(`${s.url}/`)).text();
  assert.match(home, /host/);
  await s.close();
});

test('/events 在文件变化时推送 changed', async () => {
  const dir = await mkdtemp(join(tmpdir(), 'aca-s2-'));
  const hostHtml = join(dir, 'host.html');
  await writeFile(hostHtml, 'x');
  const s = await startStudio({ gameDir: dir, hostHtmlPath: hostHtml });

  const ctrl = new AbortController();
  const res = await fetch(`${s.url}/events`, { signal: ctrl.signal });
  const reader = res.body.getReader();
  await wait(300);
  await writeFile(join(dir, 'main.ts'), 'changed-now');
  const { value } = await reader.read();
  assert.match(new TextDecoder().decode(value), /changed/);
  ctrl.abort();
  await s.close();
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `node --test tests/server.test.js`
Expected: FAIL（找不到模块）

- [ ] **Step 3: 写最小实现**

```js
// src/studio/server.js
import { createServer } from 'node:http';
import { readFileSync } from 'node:fs';
import { readProject, writeProject } from '../project-io.js';
import { createWatcher } from './watcher.js';

export function startStudio({ gameDir, port = 0, hostHtmlPath }) {
  const clients = new Set();
  const watcher = createWatcher(gameDir, () => {
    for (const res of clients) res.write('data: changed\n\n');
  });

  const server = createServer((req, res) => {
    const url = req.url.split('?')[0];
    if (req.method === 'GET' && url === '/') {
      res.writeHead(200, { 'content-type': 'text/html; charset=utf-8' });
      res.end(readFileSync(hostHtmlPath));
    } else if (req.method === 'GET' && url === '/api/project') {
      res.writeHead(200, { 'content-type': 'application/json' });
      res.end(JSON.stringify({ files: readProject(gameDir) }));
    } else if (req.method === 'POST' && url === '/api/save') {
      let body = '';
      req.on('data', (c) => (body += c));
      req.on('end', () => {
        try {
          const { files } = JSON.parse(body || '{}');
          watcher.pause(600);
          writeProject(gameDir, files);
          res.writeHead(200, { 'content-type': 'application/json' });
          res.end(JSON.stringify({ success: true }));
        } catch (e) {
          res.writeHead(500); res.end(String(e));
        }
      });
    } else if (req.method === 'GET' && url === '/events') {
      res.writeHead(200, {
        'content-type': 'text/event-stream',
        'cache-control': 'no-cache',
        connection: 'keep-alive',
      });
      res.write('retry: 1000\n\n');
      clients.add(res);
      req.on('close', () => clients.delete(res));
    } else {
      res.writeHead(404); res.end('Not Found');
    }
  });

  return new Promise((resolve) => {
    server.listen(port, '127.0.0.1', () => {
      const { port: p } = server.address();
      resolve({
        url: `http://127.0.0.1:${p}`,
        port: p,
        async close() { for (const r of clients) r.end(); await watcher.close(); server.close(); },
      });
    });
  });
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `node --test tests/server.test.js`
Expected: PASS（2 tests）

- [ ] **Step 5: 提交**

```bash
git add src/studio/server.js tests/server.test.js
git commit -m "feat: studio 服务（api + SSE）"
```

---

## Task 7: host 页（workspacesync + SSE 自动 reload）

**Files:**
- Create: `src/host/index.html`
- Reference: `archived/spike/index.html`（已验证的同步逻辑，移植并改造）
- Test: 由 Task 9 的 `aca check` 集成验证（浏览器逻辑无单元测试）

**Interfaces:**
- Consumes: studio 服务的 `/api/project`、`/api/save`、`/events`（Task 6）。
- Produces: 一个独立 HTML，被 `startStudio` 当 `hostHtmlPath` 托管。

- [ ] **Step 1: 写 host 页**（移植 `archived/spike/index.html` 的 workspacesync/workspacesave 逻辑，做三处改造：① 接口名改 `/api/project`、`/api/save`；② 固定 `src/host` 不带 mode 切换，iframe.src 用根域名；③ 增加 EventSource 监听 `/events` 收到 `changed` 时 `iframe.src = iframe.src` 强制 reload）

```html
<!doctype html>
<html lang="zh-CN">
<head><meta charset="utf-8"><title>arcade-ai studio</title>
<style>html,body{margin:0;height:100%}iframe{width:100%;height:100%;border:0}</style></head>
<body>
<iframe id="mc" src="https://arcade.makecode.com/?controller=1"
  sandbox="allow-popups allow-forms allow-scripts allow-same-origin"
  allow="autoplay; gamepad; midi"></iframe>
<script>
const iframe = document.getElementById('mc');

window.addEventListener('message', async (event) => {
  if (event.source !== iframe.contentWindow) return;
  const msg = event.data; if (!msg) return;

  if (msg.type === 'pxthost' && msg.action === 'workspacesync') {
    const data = await (await fetch('/api/project')).json();
    msg.projects = [{
      header: {
        id: 'arcade-ai-project', name: 'arcade-game',
        modificationTime: Date.now(), recentUse: Date.now(),
        target: 'arcade', editor: 'blocksprj'        // 坑3
      },
      text: data.files
    }];
    iframe.contentWindow.postMessage(msg, '*');
  }

  if (msg.type === 'pxthost' && msg.action === 'workspacesave') {
    const project = msg.project;
    if (project && project.text) {
      await fetch('/api/save', {
        method: 'POST', headers: { 'content-type': 'application/json' },
        body: JSON.stringify({ files: project.text })
      });
    }
  }
});

// 文件被外部（AI）改动 → 自动 reload iframe，重走 workspacesync（已验证路径）
const es = new EventSource('/events');
es.onmessage = (e) => { if (e.data === 'changed') iframe.src = iframe.src; };
</script>
</body>
</html>
```

- [ ] **Step 2: 人工冒烟（无自动断言，集成测试在 Task 9）**

Run: 临时手测 —
```bash
node -e "import('./src/studio/server.js').then(async m=>{const s=await m.startStudio({gameDir:'./src/template/game',hostHtmlPath:'./src/host/index.html'});console.log(s.url)})"
```
Expected: 打印 URL；浏览器打开后官方编辑器加载、出现模板里的 player 精灵。（确认后 Ctrl-C）

- [ ] **Step 3: 提交**

```bash
git add src/host/index.html
git commit -m "feat: host 页（workspacesync + SSE 自动 reload）"
```

---

## Task 8: dev 命令

**Files:**
- Modify: `src/commands/dev.js`
- Test: `tests/dev.test.js`

**Interfaces:**
- Consumes: `startStudio`（Task 6）、host 页路径。
- Produces: `runDev(ctx)` 解析 `--port`（默认 8080）、`gameDir = ./game`，起 studio，打印 URL，尝试开浏览器（失败仅警告）。返回值：测试模式（`ctx.options._noBlock`）下起服务后 resolve `{ close }`；正常模式常驻不 resolve。

- [ ] **Step 1: 写失败测试**

```js
// tests/dev.test.js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { mkdtemp, mkdir, writeFile } from 'node:fs/promises';
import { tmpdir } from 'node:os';
import { join } from 'node:path';
import runDev from '../src/commands/dev.js';

test('dev 起服务并能取到项目', async () => {
  const dir = await mkdtemp(join(tmpdir(), 'aca-dev-'));
  await mkdir(join(dir, 'game'));
  await writeFile(join(dir, 'game/main.ts'), 'hi');
  const handle = await runDev({ positionals: [], options: { _noBlock: true, _cwd: dir } });
  const proj = await (await fetch(`${handle.url}/api/project`)).json();
  assert.equal(proj.files['main.ts'], 'hi');
  await handle.close();
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `node --test tests/dev.test.js`
Expected: FAIL（stub 返回 0，无 `.url`）

- [ ] **Step 3: 写实现**

```js
// src/commands/dev.js
import { join, dirname } from 'node:path';
import { fileURLToPath } from 'node:url';
import { spawn } from 'node:child_process';
import { startStudio } from '../studio/server.js';

const here = dirname(fileURLToPath(import.meta.url));
const hostHtmlPath = join(here, '..', 'host', 'index.html');

function openBrowser(url) {
  const cmd = process.platform === 'darwin' ? 'open'
    : process.platform === 'win32' ? 'cmd' : 'xdg-open';
  const args = process.platform === 'win32' ? ['/c', 'start', '', url] : [url];
  try { spawn(cmd, args, { stdio: 'ignore', detached: true }).unref(); } catch {}
}

export default async function dev(ctx) {
  const cwd = ctx.options._cwd || process.cwd();
  const gameDir = join(cwd, 'game');
  const port = ctx.options._noBlock ? 0 : Number(ctx.options.port || 8080);
  const studio = await startStudio({ gameDir, port, hostHtmlPath });
  if (ctx.options._noBlock) return studio;
  console.log(`🎮 studio: ${studio.url}`);
  openBrowser(studio.url);
  return new Promise(() => {});   // 常驻
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `node --test tests/dev.test.js`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add src/commands/dev.js tests/dev.test.js
git commit -m "feat: aca dev 命令"
```

---

## Task 9: check 命令（puppeteer 自检）

**Files:**
- Modify: `src/commands/check.js`
- Test: `tests/check.test.js`（标 `{ skip: !process.env.ACA_E2E }`，需网络+chromium）

**Interfaces:**
- Consumes: `runDev`（起本地 studio）、`puppeteer`（懒加载，optional）。
- Produces: `runCheck(ctx)`：起 studio → puppeteer 打开 host 页 → 等编辑器握手（页面收到 `workspacesync` 并完成推送）→ 返回 0/1。puppeteer 缺失时打印安装提示返回 2。

- [ ] **Step 1: 写测试（默认 skip）**

```js
// tests/check.test.js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import runCheck from '../src/commands/check.js';

test('check 对官方编辑器握手成功', { skip: !process.env.ACA_E2E }, async () => {
  const code = await runCheck({ positionals: [], options: {} });
  assert.equal(code, 0);
});
```

- [ ] **Step 2: 运行确认（默认 skip 通过，不失败）**

Run: `node --test tests/check.test.js`
Expected: PASS（test skipped）

- [ ] **Step 3: 写实现**

```js
// src/commands/check.js
import { join, dirname } from 'node:path';
import { fileURLToPath } from 'node:url';
import { startStudio } from '../studio/server.js';

const here = dirname(fileURLToPath(import.meta.url));
const hostHtmlPath = join(here, '..', 'host', 'index.html');
const gameDir = join(here, 'template', 'game');   // 用内置模板当样本

export default async function check(ctx) {
  let puppeteer;
  try { puppeteer = (await import('puppeteer')).default; }
  catch { console.error('需要 puppeteer：npm i -D puppeteer'); return 2; }

  const studio = await startStudio({ gameDir, port: 0, hostHtmlPath });
  const browser = await puppeteer.launch({ headless: 'new', args: ['--no-sandbox'] });
  try {
    const page = await browser.newPage();
    // 在页面注入钩子：捕获 host 页向 iframe 回发 projects 的时刻
    await page.exposeFunction('__acaSynced', () => {});
    await page.evaluateOnNewDocument(() => {
      const orig = window.postMessage;
    });
    let synced = false;
    page.on('console', (m) => { if (m.text().includes('workspacesync')) synced = true; });
    await page.goto(studio.url, { waitUntil: 'networkidle2', timeout: 60000 });
    // 等待 host 页处理 workspacesync：检测是否成功发起了 /api/project 请求
    await page.waitForResponse((r) => r.url().endsWith('/api/project'), { timeout: 45000 });
    console.log('✅ 协议自检通过：编辑器已发起 workspacesync 并拉取本地项目');
    return 0;
  } catch (e) {
    console.error(`❌ 自检失败：${e.message}`);
    return 1;
  } finally {
    await browser.close();
    await studio.close();
  }
}
```

> 注：判定信号用「host 页是否被官方编辑器触发 `workspacesync` 从而请求 `/api/project`」。这是双向链路的关键握手点，比截图更可靠。`page.on('console')` 行保留作调试，主断言是 `waitForResponse(/api/project)`。

- [ ] **Step 4: 真机验证（手动，需网络）**

Run: `ACA_E2E=1 node --test tests/check.test.js`
Expected: PASS（约 30–60s，打印「协议自检通过」）

- [ ] **Step 5: 提交**

```bash
git add src/commands/check.js tests/check.test.js
git commit -m "feat: aca check（puppeteer 协议自检）"
```

---

## Task 10: 参考手册 reference/

**Files:**
- Create: `src/reference/arcade-api.md`, `limits.md`, `pitfalls.md`, `project-format.md`
- Test: `tests/reference.test.js`

**Interfaces:**
- Consumes: 内容来源——`archived/spike/README.md`、`archived/spike/report.md`、`archived/spike/development_and_troubleshooting_guide.md`。
- Produces: 四个非空 md；`pitfalls.md` 含 4 个坑关键词。

- [ ] **Step 1: 写失败测试**

```js
// tests/reference.test.js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { readFileSync, existsSync } from 'node:fs';
import { join, dirname } from 'node:path';
import { fileURLToPath } from 'node:url';

const dir = join(dirname(fileURLToPath(import.meta.url)), '..', 'src', 'reference');

test('四份参考文档存在且非空', () => {
  for (const f of ['arcade-api.md','limits.md','pitfalls.md','project-format.md']) {
    const p = join(dir, f);
    assert.ok(existsSync(p), `缺 ${f}`);
    assert.ok(readFileSync(p, 'utf8').trim().length > 50, `${f} 太短`);
  }
});

test('pitfalls.md 覆盖四个坑', () => {
  const t = readFileSync(join(dir, 'pitfalls.md'), 'utf8');
  for (const kw of ['assets.json', 'files', 'blocksprj', 'controller=1']) {
    assert.ok(t.includes(kw), `pitfalls 缺关键词 ${kw}`);
  }
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `node --test tests/reference.test.js`
Expected: FAIL（文件不存在）

- [ ] **Step 3: 写四份文档**（从 archived/spike 提炼）
- `pitfalls.md`：逐条写 4 个坑（必须出现关键词 `assets.json` 必须 `{}`、`pxt.json` 的 `files`、`editor: "blocksprj"`、`?controller=1`）+ postMessage 协议字段表（`pxthost`/`workspacesync`/`workspacesave`）。
- `arcade-api.md`：sprites/controller/game/tilemap/music/info 高频 API（从官方 + 模板 main.ts 提炼）。
- `limits.md`：屏幕 160×120、16 色调色板、sprite 数量与内存、不支持的 JS 特性（如部分 async）。
- `project-format.md`：`pxt.json` 字段、`assets.json` F4 格式、`*.g.ts` 生成、文件白名单。

- [ ] **Step 4: 运行测试确认通过**

Run: `node --test tests/reference.test.js`
Expected: PASS（2 tests）

- [ ] **Step 5: 提交**

```bash
git add src/reference/ tests/reference.test.js
git commit -m "docs: 内置精简参考手册"
```

---

## Task 11: SKILL.md、README、打包元数据

**Files:**
- Create: `SKILL.md`
- Create: `README.md`
- Modify: `package.json`（确认 `files` 含 `src`，使 reference/template/host 随包发布）
- Test: `tests/package.test.js`

**Interfaces:**
- Consumes: 全部既有命令。
- Produces: 可发布包；`SKILL.md` 描述何时用、怎么调 `aca`、何处查 reference；README 给分工具安装。

- [ ] **Step 1: 写失败测试**

```js
// tests/package.test.js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { readFileSync, existsSync } from 'node:fs';

test('SKILL.md 含 frontmatter 与命令', () => {
  const t = readFileSync('SKILL.md', 'utf8');
  assert.match(t, /^---/);
  assert.match(t, /name:/);
  assert.match(t, /aca (init|dev|check)/);
});

test('README 含三工具安装', () => {
  const t = readFileSync('README.md', 'utf8');
  for (const kw of ['plugin marketplace', '.trae', 'AGENTS.md']) {
    assert.ok(t.includes(kw), `README 缺 ${kw}`);
  }
});

test('package.json files 含 src', () => {
  const p = JSON.parse(readFileSync('package.json', 'utf8'));
  assert.ok(p.files.includes('src'));
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `node --test tests/package.test.js`
Expected: FAIL（SKILL.md 不存在）

- [ ] **Step 3: 写 SKILL.md**

```markdown
---
name: arcade-ai
description: Use when building or modifying a Microsoft MakeCode Arcade game — scaffolds a project, runs a live studio embedding the official editor, and provides an offline API/limits reference.
---

# MakeCode Arcade AI Studio

帮助用 AI 开发 MakeCode Arcade 游戏。

## 何时用
- 用户想做 / 改 arcade 游戏、像素小游戏、makecode 游戏时。

## 怎么用
1. 起项目：`npx arcade-ai init <dir>`（已固化 4 个常见坑）。
2. 起 studio：在项目目录 `npx aca dev`，浏览器实时预览，AI 改 `game/main.ts` 自动刷新。
3. 协议自检：`npx aca check`（官方编辑器握手是否仍通）。

## 写代码规则
- 只改 `game/main.ts`；资源走 `game/assets.json`。
- 写前查随包 `reference/`：`arcade-api.md`（API）、`limits.md`（硬限制）、`pitfalls.md`（坑）、`project-format.md`（工程格式）。
- 不要臆造 arcade 不存在的 API。
```

- [ ] **Step 4: 写 README.md**（含分工具安装：Claude Code 插件市场 / Trae `.trae/skills` / 通用 `AGENTS.md`；以及 `aca init|dev|check` 用法）。必须出现字符串 `plugin marketplace`、`.trae`、`AGENTS.md`。

- [ ] **Step 5: 确认 package.json**

`files` 已含 `["bin","src","SKILL.md","README.md"]`（Task 1 已设）。验证 `npm pack --dry-run` 包含 `src/reference`、`src/template`、`src/host`。

Run: `npm pack --dry-run 2>&1 | grep -E 'reference|template|host/index'`
Expected: 三类路径都出现。

- [ ] **Step 6: 运行测试确认通过**

Run: `node --test tests/package.test.js`
Expected: PASS（3 tests）

- [ ] **Step 7: 全量测试 + 提交**

```bash
node --test
git add SKILL.md README.md package.json tests/package.test.js
git commit -m "docs: SKILL.md、README（分工具安装）与打包元数据"
```

---

## Self-Review

**Spec coverage（逐节核对）：**
- §3 两层结构 / 单包三命令 → Task 1（CLI）、3/4（init）、8（dev）、9（check）✓
- §3 稳定性：init 纯拷贝无 pxt → Task 3 ✓；版本锁定 → README/SKILL（Task 11）说明 ✓
- §4 脚手架结构（game/ + 4 坑 + package.json + 规则文件）→ Task 3/4 ✓
- §4 资源走 assets.json 不内联 → 规则文件（Task 4）+ project-io 白名单（Task 2）✓
- §5 同步链路（/api/project、/api/save、SSE、reload iframe、workspacesave 回写）→ Task 6/7 ✓
- §6 自检探针 → Task 9 ✓
- §7 reference 四文档 → Task 10 ✓
- §8 第一版范围（含多格式规则、README）→ Task 4/11 ✓；明确不做二次推送/抓文档/PNG 转换（无对应 Task，符合 YAGNI）✓
- §9 分发与跨工具（多格式规则、分工具 README）→ Task 4/11 ✓
- §10 风险：写盘回声 → watcher.pause（Task 5）+ save 时 pause（Task 6）✓

**Placeholder 扫描：** Task 7/10/11 有少量"按来源提炼"的内容性步骤（host 页已给完整代码；reference/README 正文为内容创作，给了明确来源与必含关键词断言把关）。无 TODO/TBD 代码占位。

**类型一致性：** `readProject/writeProject`、`createWatcher(.close/.pause)`、`startStudio(.url/.port/.close)`、命令默认导出签名 `(ctx)→number|handle` 在各 Task 间一致。

**已知取舍：** `check` 的判定信号定为「编辑器触发 workspacesync → host 页请求 `/api/project`」，比截图稳；真机用例默认 skip，靠 `ACA_E2E=1` 手动跑。
